# Shared with mojo_business — same Supabase project + same anon key (per Q1).
# Copy values from ~/Documents/GitHub/mojo_business/.env.local into a local .env.local
# (which is gitignored).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional — only required for the /location route (Step 9 onward).
VITE_GOOGLE_MAPS_API_KEY=
